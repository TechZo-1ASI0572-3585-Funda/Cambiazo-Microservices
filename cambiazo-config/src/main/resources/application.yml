server:
  port: 8888

spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:/app/configurations
